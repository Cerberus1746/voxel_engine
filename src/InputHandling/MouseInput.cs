using Veldrid;

namespace VoxelEngine.InputHandling
{
  public class MouseInput: InputHandlingBase
  {
    public new void Update(ref InputSnapshot currentWindowEvents) {
      base.Update(ref currentWindowEvents);
    }
  }
}
